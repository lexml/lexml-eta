## 1. Reproduzir o bug com teste unitário

- [x] 1.1 Adicionar teste dedicado em `test/model/remissao/` cobrindo: parágrafo único com remissão apontando para ele, dispara `renumeraFilhos`/sincronização por uma ação estrutural em OUTRA parte do documento (ex.: novo artigo inserido antes), e o texto da remissão deveria permanecer "parágrafo único" — rodar o teste ANTES de qualquer mudança em `idUtil.ts` e confirmar que ele falha (reproduz o bug relatado na issue #1003), deixando o teste no arquivo (não revertê-lo)
- [x] 1.2 Corrigir a asserção do cenário pré-existente "reflete mudança estrutural imediatamente" em `test/model/remissao/textoCanonicoDoDispositivo.test.ts:95-110`, que hoje espera `'§ 1º do art. 1º'`, para esperar `'parágrafo único do art. 1º'` — rodar e confirmar que também falha nesse ponto (segunda evidência do mesmo bug), sem alterar produção ainda

## 2. Persistir a contagem ao vivo no ponto de escrita existente (`createRotulo`)

- [x] 2.1 (Tentativa descartada, mantida no histórico) Recontar ao vivo diretamente em `buildHref`/`idUtil.ts` — refutado por evidência de corpus real (documentos legislativos divergem nas duas direções, dentro e fora de bloco de alteração); `idUtil.ts` foi revertido ao original. Ver `design.md`, seção "Decisão A tentada e descartada".
- [x] 2.2 Reutilizar `isParagrafoUnico` (`src/model/lexml/hierarquia/hierarquiaUtil.ts`) nas duas ramificações de `createRotulo` (`numeracaoParagrafo.ts`) que já recontam ao vivo para decidir o rótulo exibido, substituindo a expressão inline duplicada `pai.filhos.filter(isParagrafo).length === 1` — e verificar que `test/model/dispositivo/paragrafo.test.ts` continua passando sem alteração (comportamento de exibição inalterado)
- [x] 2.3 Nessas mesmas duas ramificações, gravar `this.informouParagrafoUnico = isParagrafoUnico(dispositivo)` antes de decidir o rótulo (persistindo o que hoje só é lido para exibição) — e confirmar que os dois testes da seção 1 (1.1 e 1.2), antes falhando, agora passam, sem tocar `idUtil.ts`

## 3. Expandir cobertura de teste unitário e validar suíte completa (incluindo corpus real)

- [x] 3.1 Adicionar teste cobrindo o caso oposto (não-regressão): parágrafo que JÁ NÃO é mais único (segundo parágrafo adicionado de fato via `criaDispositivo` + `pai.renumeraFilhos()`) tem sua remissão corretamente atualizada para a forma numerada ("§ 1º", "§ 2º") — verificar que passa
- [x] 3.2 Adicionar teste cobrindo a transição inversa: remover o segundo de dois parágrafos (deixando 1) faz o remanescente voltar a "único" — verificar que passa
- [x] 3.3 Rodar a suíte completa (`npm test`) e confirmar, em especial, que `test/model/documento/buildJsonixFromProjetoNorma/buildJsonixFromProjetoNorma.integracao.test.ts` (round-trip com corpus real: MPV 885/2019, MPV 905/2019, MPV 930/2020, MPV 1085/2021, MPV 1100/2022, PLP 68/2024, PL 4687/2023, PEC 48/2023) continua 100% idêntico ao original — esse é o teste que teria pego a regressão da tentativa A, e é a verificação mais forte de que a Decisão B não introduz o mesmo problema. Confirmado: `npm test` completo (todos os lotes) passou com exit code 0.
- [x] 3.4 Rodar também `test/redux/remissao/reducer-deteccao-contextual.test.ts` e `reducer-remove-remissao-renumeracao.test.ts` (fixtures com parágrafo/artigo únicos criados ao vivo em contexto de detecção, não de carregamento) e confirmar que nenhuma asserção pré-existente quebrou
- [x] 3.5 Corrigir 10 asserções pré-existentes, em 6 arquivos de teste (`textoCanonicoRelativoATipo.test.ts`, `sincronizarRemissoesComEstadoAtual.test.ts`, `reducer-deteccao-composta.test.ts`, `reducer-deteccao-contextual.test.ts`, `reducer-remove-remissao-renumeracao.test.ts`), que usavam incidentalmente fixtures com parágrafo/artigo único (não relacionadas ao eixo único/numerado) — corrigidas com a mesma técnica de preenchimento de irmão (`criaDispositivo` de um segundo parágrafo), preservando a asserção numérica original de cada teste

## 4. Teste E2E (Cypress)

- [x] 4.1 Adicionar `CT-H-03` em `cypress/e2e/remissao-interna/grupo-h-referencia-enxuta.cy.ts` (mesmo padrão de `CT-H-01`/`CT-H-02`, describe "D4"): criar artigo com um único parágrafo, criar remissão "parágrafo único" a partir de outro dispositivo, inserir um novo artigo alheio antes (disparando renumeração/sincronização global), e verificar que o texto do link permanece "parágrafo único" — cobre a regressão relatada na issue #1003 ponta a ponta, incluindo o gatilho real de sincronização pós-ação (`sincronizarRemissoesPosAcao.ts`) que os testes unitários da seção 1-3 não exercitam. Origem precisou ficar DENTRO do próprio Art. 1 (inciso no caput), não em outro artigo — achado real durante a escrita: "parágrafo único" bare É reconhecível pela detecção implícita (Etapa 1.5), e uma origem em outro artigo sem parágrafo próprio faz essa redetecção (disparada ao clicar no menu de outro dispositivo) substituir o registro de remissões por um vazio ("sempre substituído, nunca acumulado"), apagando o link injetado manualmente antes da asserção final — não é um bug da correção em si, é uma característica pré-existente do motor de detecção que também afeta CT-H-01 (que evita o problema porque origem e destino já estão no mesmo artigo).
- [x] 4.2 Rodar o spec isoladamente (`npm run cy:run:local -- --spec cypress/e2e/remissao-interna/grupo-h-referencia-enxuta.cy.ts`) e confirmar que `CT-H-03` passa junto com `CT-H-01`/`CT-H-02` (sem regressão nos casos já cobertos). Confirmado: 3/3 passando.

## 5. Atualizar spec principal

- [ ] 5.1 Após validação do usuário, arquivar a change (`openspec archive`) para mesclar o delta de `specs/remissao-interna/spec.md` na spec principal em `openspec/specs/remissao-interna/spec.md`
