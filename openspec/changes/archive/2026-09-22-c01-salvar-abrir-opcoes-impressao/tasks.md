## 1. Modelo e padrão de composição dos metadados do LexEdit

- [x] 1.1 Trocar o import de `OpcoesImpressao` em `opcoesImpressao.component.ts` e `lexml-eta.component.ts` de `model/emenda/emenda` para `model/proposicao/proposicao`, e verificar que `npm run build` compila sem erros.
- [x] 1.2 Em `documentoArticulado.ts`, adicionar `opcoesImpressao?` (quatro atributos opcionais) a `MetadadoLexEdit` e criar o tipo `DadosLexEdit` (`opcoesImpressao?: OpcoesImpressao`), e verificar que `npm run build` compila sem erros.
- [x] 1.3 Em `buildJsonixFromProjetoNorma.ts`, criar o ponto de composição `montaMetadadoLexEdit(dados, idsRemissoesInvalidas)`, que devolve `undefined` quando não há grupo, e fazer `montaMetadadoProprietario` apenas embrulhar o `lexedit` composto. Manter intacto o formato das remissões inválidas e verificar que os testes existentes de `MetadadoProprietario — remissões internas inválidas` (`buildJsonixFromProjetoNorma.content.test.ts`) continuam passando.

## 2. Salvar opções de impressão

- [x] 2.1 Implementar o montador `montaOpcoesImpressao` (grava sempre os quatro atributos, copiando valores) e propagar `dados?: DadosLexEdit` por `buildJsonixFromProjetoNorma` e `criarDocumentoArticulado` como último parâmetro opcional. Verificar com testes unitários: os quatro atributos presentes com valores alterados e com valores padrão; documento criado sem `dados` e sem remissão inválida continua sem `MetadadoProprietario`; opções de impressão e remissão inválida no mesmo `MetadadoProprietario`; alterar o objeto de origem depois de criar o documento não altera o documento.
- [x] 2.2 Fazer `lexml-eta.component.ts#getDocumentoArticulado` coletar as opções de impressão do formulário (`_lexmlOpcoesImpressao`) e repassá-las a `lexml-eta-proposicao.component.ts#getDocumentoArticulado`, que as entrega a `criarDocumentoArticulado`. Verificar com teste de componente, ou manualmente no demo pelo botão Salvar, que o arquivo gerado contém `lexedit.opcoesImpressao` com os valores do formulário.

## 3. Abrir opções de impressão

- [x] 3.1 Em `buildProjetoNormaFromJsonix.ts`, implementar `lerOpcoesImpressao` (parte de `new OpcoesImpressao()` e sobrepõe cada atributo presente e com tipo válido; `tamanhoFonte` aceita qualquer inteiro positivo) e `lerMetadadoLexEdit(documento): DadosLexEdit`, ambos tolerantes a grupos desconhecidos. Verificar com testes unitários: todos os atributos; somente parte deles; `tamanhoFonte` zero e não numérico; `imprimirBrasao` como string; sem `MetadadoProprietario`; sem o grupo; grupo junto com remissões inválidas e com um grupo desconhecido.
- [x] 3.2 Em `lexml-eta.component.ts#abrirDocumentoArticulado`, ler `lerMetadadoLexEdit(documento)` e, depois de `inicializarEdicao`, aplicar `opcoesImpressao` ao formulário quando o grupo existir. Verificar manualmente no demo que abrir um arquivo com opções alteradas exibe os valores no formulário e que abrir um arquivo sem o grupo exibe os padrões.
- [x] 3.3 Verificar com teste unitário a ida e volta pelo código do editor: `criarDocumentoArticulado` com opções alteradas → `lerDocumentoArticulado` → `lerMetadadoLexEdit` devolve as mesmas opções.
- [x] 3.4 Corrigir o eco do `sl-select` em `OpcoesImpressaoComponent`: o Shoelace `2.0.0-beta.73` emite `sl-change` de forma assíncrona também quando o valor muda por código, e `_atualizarTamanhoFonte` gravava o `tamanhoFonte` anterior no objeto atribuído em seguida (ex.: abrir um arquivo sem o grupo depois de outro com fonte 18 mantinha 18). O componente passa a ignorar `sl-change` cujo valor seja o último que ele mesmo exibiu. Verificar com o teste de componente que abre um arquivo com opções e depois um sem o grupo.

## 4. Testes de integração e E2E

- [x] 4.1 Em `documentoArticulado.integration.ts`, adicionar um cenário que cria o documento com opções de impressão e faz a ida e volta pelo CLI real (`toxml` + XSD + `tojson`), verificando a presença de `<MetadadoProprietario fonte="http://www.lexml.gov.br/lexedit/1.0"` e comparando todo o resto sem o `lexedit`, com uma asserção que registra a limitação do CLI atual (design.md, Decisão 6). Os três cenários de ida e volta existentes ficam como estão: eles criam o documento sem dados do LexEdit, então continuam sem `MetadadoProprietario` e não são afetados. Verificar rodando a suíte com `JSONIX_LEXML_CLI` apontando para o `jsonix-lexml` disponível.
- [x] 4.2 Gerar a fixture `demo/doc/teste_opcoes_impressao.json` via `criarDocumentoArticulado` (não à mão), com opções de impressão diferentes do padrão, e adicionar o spec Cypress `cypress/e2e/documento-articulado/abertura-opcoes-impressao.cy.ts`. Ele abre a fixture pela UI real (`cy.get('#fileUpload').selectFile(...)`) e verifica, no `lexml-eta-opcoes-impressao`, o checkbox de brasão, o texto do cabeçalho, o tamanho de letra e o checkbox de espaço entre linhas. Consultar `docs/guia-cypress.md` (Shadow DOM, abas). O lado de salvar não é coberto por E2E: sem infraestrutura de download no projeto, mesma decisão da change de remissões inválidas; esse risco fica coberto pelas tasks 2.1, 3.3 e 4.1. Verificar rodando o spec com `npm run cy:run:local`.

## 5. Regressão e fechamento

- [x] 5.1 Rodar `npm test` e o spec Cypress `abertura-remissao-invalida-persistida.cy.ts`, e confirmar que nenhum teste de salvar/abrir ou de remissões inválidas regrediu.
- [x] 5.2 Atualizar a tabela de status de `docs/extensao-formato-lexml/plano-xsd-lexedit.md` (grupo `02` passa a Implementado) e registrar em `CLAUDE.md` o padrão de composição por grupo e a limitação do CLI em ida e volta.
